ADMIN_SERVICE_HOST=${ADMIN_SERVICE_HOST}
ADMIN_SERVICE_PORT=${ADMIN_SERVICE_PORT}

DATABASE_SERVICE_HOST=${DATABASE_SERVICE_HOST}
DATABASE_SERVICE_PORT=${DATABASE_SERVICE_PORT}

HEADWAY_GITHUB_OAUTH_CLIENT_ID=${HEADWAY_GITHUB_OAUTH_CLIENT_ID}
HEADWAY_GITHUB_OAUTH_CLIENT_SECRET=${HEADWAY_GITHUB_OAUTH_CLIENT_SECRET}
# GitHub OAuth App: add callback URL {ORIGIN}/internal/v1/admin/auth/github/callback (e.g. http://localhost:8083/...).
# If registered URL uses a different host than the browser (localhost vs 127.0.0.1), set origin explicitly:
# HEADWAY_ADMIN_OAUTH_PUBLIC_ORIGIN=http://localhost:8083
HEADWAY_REPO_OWNER=${HEADWAY_REPO_OWNER}
HEADWAY_REPO_NAME=${HEADWAY_REPO_NAME}
HEADWAY_ADMIN_SESSION_SECRET=${HEADWAY_ADMIN_SESSION_SECRET}
