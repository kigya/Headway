package dev.kigya.headway.gateway.core.exception

internal enum class GatewayErrorReason {
    BAD_REQUEST,
    EMPTY_ID_TOKEN,
    EMPTY_FINGERPRINT,
    EMPTY_REFRESH_TOKEN,
    INTERNAL_ERROR,
    MISSING_AUTH_HEADER,
    MALFORMED_BEARER_HEADER,
    INVALID_ACCESS_TOKEN,
    ACCOUNT_INACTIVE,
    ACCESS_TOKEN_EXPIRED,
    INVALID_GUEST_TOKEN,
    GUEST_TOKEN_EXPIRED,
    INVALID_REFRESH_TOKEN,
    INSUFFICIENT_ROLE,
    GUEST_NOT_ALLOWED,
    USER_NOT_INVITED,
    GOOGLE_EMAIL_NOT_VERIFIED,
    IDENTITY_CONFLICT,
    DEPENDENCY_FAILURE,
    UPSTREAM_PROTOCOL,
    NOT_FOUND,
}
