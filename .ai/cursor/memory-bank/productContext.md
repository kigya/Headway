# Product context

Facts from repo docs only:

- Public docs and onboarding are maintained in **GitBook**; editable sources are under `.gitbook/`.
- Quickstart lists the monorepo split: `client/` (KMP app), `server/` (Ktor services), shared Kotlin policy in `AGENTS.md` files.

## Open questions

- Product goals, roadmap, and release naming are **not** defined in this Memory Bank; confirm with the team or issue tracker when it matters for a task.
